{
  "name": "emt",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "turbo run dev",
    "build": "pnpm build:agent && turbo run build",
    "build:agent": "cargo build --release -p agent-tools && node scripts/build-agent.js"
  },
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@11.1.2",
  "devDependencies": {
    "@types/node": "^25.6.2",
    "tsx": "^4.21.0",
    "turbo": "^2.9.12",
    "typescript": "^6.0.3",
    "wait-on": "^9.0.5"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
